meta {
  name: Update Inventory
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/api/inventories/{{inventoryId}}
  body: json
  auth: none
}

headers {
  cookie: {{sessionCookie}}
}

body:json {
  {
    "name": "Inventaire Base Nord (updated)"
  }
}

meta {
  name: Create Inventory
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/inventories
  body: json
  auth: none
}

headers {
  cookie: {{sessionCookie}}
}

body:json {
  {
    "name": "Inventaire Base Nord",
    "accessCode": "NORD-2024",
    "ownerId": "{{userId}}"
  }
}

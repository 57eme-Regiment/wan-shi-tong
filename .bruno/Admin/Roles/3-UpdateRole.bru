meta {
  name: Update Role
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/admin/roles/{{roleId}}
  body: json
  auth: none
}

headers {
  cookie: {{sessionCookie}}
}

body:json {
  {
    "name": "Stock User (updated)",
    "description": "Description mise à jour"
  }
}

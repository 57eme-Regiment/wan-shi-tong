meta {
  name: Create Permission
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/admin/permissions
  body: json
  auth: none
}

headers {
  cookie: {{sessionCookie}}
}

body:json {
  {
    "key": "stock:inventory:export",
    "description": "Exporter les données d'un inventaire"
  }
}

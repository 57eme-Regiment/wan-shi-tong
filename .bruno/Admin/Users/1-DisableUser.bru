meta {
  name: Disable User
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/admin/users/{{userId}}/disable
  body: json
  auth: none
}

headers {
  cookie: {{sessionCookie}}
}

body:json {
  {
    "reason": "Comportement inapproprié"
  }
}

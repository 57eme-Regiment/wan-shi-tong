meta {
  name: Check Permission
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/authorize
  body: json
  auth: none
}

headers {
  cookie: {{sessionCookie}}
}

body:json {
  {
    "permission": "{{permissionKey}}"
  }
}

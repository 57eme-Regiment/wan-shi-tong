generator client {
  provider        = "prisma-client"
  output          = "../../src/generated"
  previewFeatures = ["postgresqlExtensions"]
}

datasource db {
  provider   = "postgresql"
  schemas    = ["auth", "droit"]
  extensions = [pg_trgm]
}